{
  catppuccin = {
    # Sets many things to catppuccin
    enable = true;
    flavor = "macchiato";
    accent = "pink";

    # Want to theme these my self because defaults are not to my liking
    swaync = {
      enable = false;
    };

    hyprlock = {
      enable = false;
    };

    foot = {
      enable = false;
    };

    rofi = {
      enable = false;
    };

    cursors = {
      enable = true;
    };
  };
}
