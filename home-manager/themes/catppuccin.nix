{
  catppuccin = {
    # Sets many things to catppuccin
    autoEnable = true;
    enable = true;
    flavor = "macchiato";
    accent = "pink";

    # Want to theme these my self because defaults are not to my liking
    foot = {
      enable = false;
    };
    cursors = {
      enable = true;
    };
  };
}
